@if(flashMessages.has('success'))
<div class="toast align-items-center text-white bg-success position-fixed top-0 end-0 m-3 border-0" role="alert"
  aria-live="assertive" aria-atomic="true" id="successToast">
  <div class="d-flex">
    <div class="toast-body">
      {{ flashMessages.get('success') }}
    </div>
    <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"
      aria-label="Close"></button>
  </div>
</div>
<script type="module">
  const toastInst = document.getElementById('successToast')
  const toast = new Toast(toastInst)
  toast.show()

</script>
@endif

@if(flashMessages.has('info'))
<div class="toast align-items-center text-white bg-info position-fixed top-0 end-0 m-3 border-0" role="alert"
  aria-live="assertive" aria-atomic="true" id="infoToast">
  <div class="d-flex">
    <div class="toast-body">
      {{ flashMessages.get('info') }}
    </div>
    <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"
      aria-label="Close"></button>
  </div>
</div>
<script type="module">
  const toastInst = document.getElementById('infoToast')
  const toast = new Toast(toastInst)
  toast.show()

</script>
@endif

@if(flashMessages.has('warning'))
<div class="toast align-items-center text-white bg-warning position-fixed top-0 end-0 m-3 border-0" role="alert"
  aria-live="assertive" aria-atomic="true" id="warningToast">
  <div class="d-flex">
    <div class="toast-body">
      {{ flashMessages.get('warning') }}
    </div>
    <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"
      aria-label="Close"></button>
  </div>
</div>
<script type="module">
  const toastInst = document.getElementById('warningToast')
  const toast = new Toast(toastInst)
  toast.show()

</script>
@endif

@if(flashMessages.has('error'))
<div class="toast align-items-center text-white bg-danger position-fixed top-0 end-0 m-3 border-0" role="alert"
  aria-live="assertive" aria-atomic="true" id="dangerToast">
  <div class="d-flex">
    <div class="toast-body">
      {{ flashMessages.get('error') }}
    </div>
    <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"
      aria-label="Close"></button>
  </div>
</div>
<script type="module">
  const toastInst = document.getElementById('dangerToast')
  const toast = new Toast(toastInst)
  toast.show()

</script>
@endif
